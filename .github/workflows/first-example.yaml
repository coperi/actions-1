name: My first Workflow
on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
          
        - name: CheckoutRepo
          uses: actions/checkout@v6

        - name: List and Read files
          run:
             echo "My first Github action"
             ls -la
             cat README.md

        - name: Genarate ASCII art
          run: cowsay -f dragon "Hello World!" >> dragon.txt


        # - name: List files
        #   run: ls

        # - name: Read file
        #   run: cat README.md
      